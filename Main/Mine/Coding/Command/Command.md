- Docker
    
    - Free space
    
    ```JavaScript
    docker system prune -a --volumes
    ```
    
    - Start the docker
    
    ```JavaScript
    docker compose up --build
    ```

  

[[Mac clear caches]]

  

docker exec -it rent-count-app-1 python /app/backend/manage.py migrate