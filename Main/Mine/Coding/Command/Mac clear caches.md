如果您想手動逐一搜尋並刪除檔案，請參考以下步驟：

1. 點擊 Finder 的「前往」選單，然後選擇「前往檔案夾」。
2. 在新視窗的搜尋框中，分別鍵入以下檔案夹路徑，然後在鍵盤上按下「Return」鍵。
    - ~/Library/Caches
    - /Library/Caches
    - /System/Library/Caches
    - ~/Library/Logs
    - /Library/Logs
    - /Users/likelitchi/Library/Containers